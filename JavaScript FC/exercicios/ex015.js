let myCourses = [
    "Learn CSS Animation", "UI Design Fundamntals", "Intro to Clean Code"
]

function render(array) {
    for(let i = 0; i < array.length; i++) {
        console.log(array[i])
    }
}

render(myCourses)